import type { NodeProps } from '@xyflow/react'
import type { NAgent } from '#/types'
import { UNode } from '..'

/**
 * # AI 输出节点
 * 展示智能体处理后的输出结果
 */
export const AIOutputNode = (props: NodeProps<NAgent>) => {

  return (
    <UNode node={props}>

    </UNode>
  )
}
