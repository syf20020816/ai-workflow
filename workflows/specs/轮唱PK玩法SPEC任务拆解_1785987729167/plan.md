# plan.md

> 占位。技术方案（概设）产物将写入此文件。

## Lark文档节点

{
  "ok": true,
  "identity": "user",
  "data": {
    "document": {
      "content": "<title>小陪伴-轮唱抢唱PK玩法 前端技术文档（PC改造部分）</title>\n\n我已对代码仓库完成了全面探索与验证。以下是基于需求分析与实际代码验证后产出的最终技术文档。\n\n---\n\n# 小陪伴-轮唱抢唱PK玩法 前端技术文档（PC改造部分）\n\n## 基础信息表\n\n| 所属业务 | 小陪伴 |\n|-|-|\n| 需求负责人 | 产品：XXX / 开发：XXX |\n| 需求文档 | [https://lizhi2021.feishu.cn/wiki/MDD1wE342iORsvkZUuVcteS9n5e](https://lizhi2021.feishu.cn/wiki/MDD1wE342iORsvkZUuVcteS9n5e) |\n| 设计稿 | [https://www.figma.com/design/hm4QqHDUQSYEK1FqJzL2Qq/%E7%9B%B4%E6%92%AD%E9%97%B43?node-id=5220-24651](https://www.figma.com/design/hm4QqHDUQSYEK1FqJzL2Qq/直播间3?node-id=5220-24651) |\n| 迭代分支 | ⚠️ **上游文档标注 `feature/live-room-sing-pk-v1.0`，但仓库中不存在该分支**（当前分支为 `dev`，仅有 `master` 与 `feature/260622_noble_v4`） |\n| 部署地址 | PC端：待填写 |\n| 接口文档 | 实际接口为 `/singLoop/*` 系列（见接口表），非上游文档所述 `/api/v1/sing-pk/*` |\n| 本次负责范围 | **仅项目1：小陪伴PC端直播间（需求1\\~4）**；Groot配置后台 / BMS用户组管理 不在本仓库范围内 |\n\n---\n\n## 项目说明（需求分解与实现说明）\n\n### ⚠️ 代码验证重要结论（与上游文档的差异点）\n\n在写修改明细前，必须先说明本次代码验证发现的关键事实，后续所有表格均以此为准：\n\n| # | 上游文档描述 | 代码验证实际结论 |\n|-|-|-|\n| 1 | 文档路径为 Vue 组件（`KeyboardTab.vue` / `SongDesk.vue` / `GameCard.vue` / `RoomFlowInfo.vue`） | **本仓库是 React + Electron + TypeScript** 项目（electron-react-boilerplate），无任何 `.vue` 文件。文档中所有组件路径均不成立 |\n| 2 | 入口外显改造（键盘面板TAB、点歌台TAB、公屏游戏卡片、房间信息流） | **实际入口只有一处**：左侧直播工具「互动玩法 → 轮唱PK」工具项（`LiveToolModule`）。键盘/点歌台/公屏/房间信息流四个点位均未实现 |\n| 3 | 轮唱PK核心布局、全流程环节交互 | **已在 `dev` 分支完整实现**，模块为 `RoundSingModule`，状态机、麦位布局、抢唱/应战/演唱/打分/结算全链路均已落地，本次工作重心应是\"对齐/收敛\"而非\"从零开发\" |\n| 4 | 玩法互斥覆盖「跨房PK/魔法团战/合唱/小游戏/心动时刻/甩地雷/抢皇冠/1V1通话」 | **实际仅覆盖**：已开启轮唱PK、跨厅PK、魔法团战、全麦PK、黑叶投票中。**1V1通话、KTV合唱、PP小游戏、蒙面、投屏等均未在发起入口拦截**，属明显缺口 |\n| 5 | 评委席：上评委席校验、引导气泡、打分权限、离席二次确认 | **评委席为纯服务端驱动**（`judgeSeats` 由业务侧上报），客户端仅渲染3个席位；无上评委席/离席确认交互，无对应 API |\n| 6 | 演唱环节快捷送礼按钮（默认文案\"为Ta打Call\"） | **实际代码中未实现**该按钮 |\n| 7 | Groot配置（轮次/各环节倒计时/礼物ID/跳转链接/动效开关） | 实际配置来源是 `/business/common/config` 下发的 `singLoopConfigs`（仅8个参数：各环节倒计时、perfect阈值、scoring等级），**无礼物ID/快捷送礼/跳转链接/动效开关配置** |\n| 8 | 接口 `/api/v1/sing-pk/start` 等 | 实际接口为 `/singLoop/start`、`/singLoop/info`、`/singLoop/round/grab` 等（见接口表） |\n\n### 需求分解\n\n| 需求 | 主题 | 实现状态 |\n|-|-|-|\n| 需求1 | 玩法入口外显改造 | ⚠️ 部分存在（工具项入口已实现，键盘/点歌台/公屏/信息流点位缺失） |\n| 需求2 | 轮唱PK核心布局实现 | ✅ 已完整实现 |\n| 需求3 | 全流程环节交互逻辑实现 | ✅ 已完整实现（快捷送礼按钮除外） |\n| 需求4 | 玩法互斥&异常兜底逻辑实现 | ⚠️ 部分实现（互斥覆盖不全，异常兜底基本齐备） |\n\n---\n\n## 组件/文件修改表\n\n> 以下所有路径均为 **实际代码路径**（`src/renderer/...`），并标注该文件当前是否已存在。\n\n### 需求1：玩法入口外显改造\n\n| 类型 | 文件路径 | 现状 | 改动说明 |\n|-|-|-|-|\n| 组件 | `/src/renderer/modules/LiveModule/submodules/LiveToolModule/hooks/useLiveTool.tsx` | ✅ 已有入口 | 轮唱PK工具项已存在（`handleRoundSingClick` → `dispatch(openRoundSing())`，icon= `assets/images/sing_loop/entry.png`，title=\"轮唱PK\"）。**如需求要求键盘/点歌台/公屏/信息流多入口，需在此之外新增点位** |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/LiveToolModule/views/index.tsx` | ✅ 已有 | 工具列表按 `hasAnyPermission(PermissionKey.轮唱PK)` 过滤，无需改 |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/PublicScreenModule/views/index.tsx`（公屏） | ✅ 已有 | 如需「公屏游戏卡片」样式（轮唱PK/听合唱），需在此新增卡片渲染分支（**当前未实现**） |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/SingModule/views/SingPanel/SingPanelContent.tsx`（点歌台） | ✅ 已有 | 当前为纯列表无TAB，如需「点歌台音乐玩法TAB」需新增（**当前未实现**） |\n| 文件 | `/src/types/permission.ts` | ✅ 已有 | `PermissionKey.轮唱PK = 'operation_sing_loop_pk'` 已定义 |\n| 文件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/services/store.ts` | ✅ 已有 | `selectHasRoundSingPermission` 已实现（读 `permission.modules` 含 `operation_sing_loop_pk`） |\n| 文件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/services/action.ts` | ✅ 已有 | `openRoundSing` 已实现发起校验（详见需求4） |\n\n**改动说明（如按上游需求新增点位）：**\n\n1. 键盘面板/输入栏（`PublicScreenModule/views/FooterContainer.tsx`）如需玩法TAB：新增 Tab 控件，配置项支持图片+名称，上新红点按设备本地存储统计点击（可参考 `useLiveTool.tsx` 中 `newFlag` + `localStorage` 模式）。\n2. 点歌台（`SingPanel`）如需音乐玩法TAB：Tab 控件默认排序第二位，支持图片+名称+描述文案。\n3. 公屏游戏卡片：新增轮唱PK/听合唱样式，右下角图标替换为玩法logo（资源 `assets/images/sing_loop/entry.png`）。\n4. 房间信息流横版轮唱PK样式（最多5位麦上用户头像）：**当前无对应组件**，需按房间信息流现有组件扩展。\n\n**权限口径说明（重要）：**\n\n- 上游文档称\"仅点唱厅展示、非官方厅仅签约主播可见、官方厅仅麦上用户可见\"。\n- **实际代码未按房间类型/签约身份做差异化**，仅两重判断：①入口可见性 = 权限 `modules` 含 `operation_sing_loop_pk`；②发起 = `isOnSeat || isHost`（`openRoundSing` 内 `getSeatSelector().isOnSeat` + `getIsHostSelector`）。若需求要求点唱厅/签约身份/官方厅白名单限定，需新增校验（可结合 `selectIsAmusementSingRoom`、`getLiveRoomType` 与后端返回身份字段）。\n\n---\n\n### 需求2：轮唱PK核心布局实现（✅ 已实现，仅列现状与需收敛点）\n\n| 类型 | 文件路径 | 现状 | 说明 |\n|-|-|-|-|\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/views/RoundSingPanel.tsx` | ✅ | 顶层容器，使用 `ViewTransition` 状态机驱动进出场（`useViewModuleState('roundSing')`） |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/views/BattleStage/index.tsx` | ✅ | 对战区：`ScoreRoundBar` + 左 `TeamPanel(A)` + 中 `CenterSeam` + 右 `TeamPanel(B)` |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/views/BattleStage/ScoreRoundBar.tsx` | ✅ | 红蓝比分条 + 回合胶囊（第x轮 x/y），极端比分锚点限幅 8%\\~92% |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/components/TeamPanel/index.tsx` | ✅ | 红/蓝队面板，内部渲染 `UserSeat`（size=`roundSingSize`）/ `EmptySeat` |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/components/CenterSeam/index.tsx` | ✅ | 评委席（3席竖向）+「轮唱PK」logo |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/components/CenterSeam/JudgeSeat/index.tsx` | ✅ | 单评委席位（仅渲染身份头像/占位，不展示打分状态） |\n| 文件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/services/store.ts` | ✅ | `selectSingLoopTeamSeatList`：`TEAM_SEAT_NUMS_MAP = { A: [0,1,4,5], B: [2,3,6,7] }`（**与上游\"红队1/2/5/6麦、蓝队3/4/7/8麦\"一致，0基/1基换算后对应**） |\n| 文件 | `/src/renderer/modules/LiveModule/submodules/SeatModule/components/ViewTransition/transitionFunc.ts` | ✅ | `SeatViewMode = 'amusement' \\| 'pk' \\| 'teamWar' \\| 'roundSing'`；上游文档所述\"枚举新增7/8\"的旧方案已重构为通用模式 |\n| 组件 | `/src/renderer/modules/LiveModule/submodules/SeatModule/views/index.tsx` | ✅ | 已挂载 `<RoundSingPanel />`，`showSingerSeat` 中 `!isRoundSingActive` 隐藏点唱厅主麦位 |\n| 组件 | `/src/renderer/modules/LiveModule/views/index.tsx` | ✅ | 轮唱PK进行中 `BroadcastModule` 下移 `top-[200px]`（即\"进场横幅向下偏移适配评委席区域\"） |\n\n**⚠️ 需关注的特殊点：**\n\n- **送礼弹道偏移**：`GiftEffectModule/views/index.tsx` 中弹道条 `top` 由 `middleTopContainerSize.height - ContainerTop + 48` 动态计算，天然随布局高度自适应，**无需额外改**；但需回归验证轮唱布局下 `middleTopContainerSize` 是否正确（依赖 `useMiddleScreenSize(true)`）。\n- **麦位声浪（声纹动画）**：`UserSeat/index.tsx` 中声纹动画渲染依赖麦位 `materialUrl`（用户佩戴声浪装扮），无\"点唱房默认声浪兜底\"逻辑，**与上游\"兜底使用点唱房默认声浪\"不一致**，如需兜底需在 `UserSeat` 增加分支。\n- 轮唱期间隐藏密友连线、离开中标签：需确认 `UserSeatStatus` / 连线组件在 `isRoundSingActive` 下是否已隐藏（当前代码未见明确处理，建议作为待办项）。\n\n---\n\n### 需求3：全流程环节交互逻辑实现（✅ 已实现，快捷送礼除外）\n\n| 类型 | 文件路径 | 现状 | 说明 |\n|-|-|-|-|\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/views/Header/index.tsx` | ✅ | 阶段路由 + 投降按钮（二次确认 `Modal.confirm`） |\n| 组件 | `views/Header/PhaseStateBar.tsx` | ✅ | 局准备/轮次准备过场/结算惩罚倒计时/轮空卡（含 `RoundStateBar` 复用） |\n| 组件 | `views/Header/PhaseCount.tsx` | ✅ | 抢唱/应战环节（`ActionButton` + 歌词），非队员展示\"红蓝抢唱中/迎战中\"文案 |\n| 组件 | `views/Header/PhaseSinging.tsx` | ✅ | 演唱环节：`PitchLine` + 歌词 + `DotCountdown` + `PerfectIndicator` + `PopularityScore` |\n| 组件 | `views/Header/PhaseScoring.tsx` | ✅ | 委托 `ScoreBoard` 渲染评分/已评分 |\n| 组件 | `views/Header/PhaseReadySuccessOverlay/` | ✅ | 抢唱成功/应战成功小卡片 + 下一首小卡片 + 音效 |\n| 组件 | `components/ActionButton/index.tsx` | ✅ | 抢唱/迎战按钮（SVG倒计时环 + 点击缩放 `active:scale-[0.92]`） |\n| 组件 | `components/PitchLine/index.tsx` | ✅ | Canvas 音高线 + 头像跟随（rAF 节流、平滑插值、高亮段匹配），读 `readLocalFileText` 解析标准JSON音高文件 |\n| 组件 | `components/ScoreBoard/index.tsx` | ✅ | 评分中（评委横向打分实时展示）/已评分（系统分/人气分/评委分/总分） |\n| 组件 | `components/PopularityScore/index.tsx` | ✅ | 人气分展示（满100显示\"已满\"标记） |\n| 组件 | `components/PerfectIndicator/index.tsx` | ✅ | 唱评（perfect标签 + 连击计数），阈值 `singLoopConfigs.perfectThreshold`（默认90） |\n| 组件 | `components/DotCountdown/index.tsx` | ✅ | READY 阶段三点倒计时，归零 dispatch `reportSingStart` |\n| 组件 | `components/SettlementModal/` | ✅ | 结算弹窗：胜负横幅 + MVP×2 + 比分明细入口 + 关闭 |\n| 组件 | `components/SettlementDetailModal/index.tsx` | ✅ | 比分明细滑动列表（系统分/评委分/人气分/总分） |\n| 组件 | `components/RoundStateBar/index.tsx` | ✅ | 惩罚环节\"提前结束\"按钮（发起者/麦上用户可见，二次确认） |\n| 文件 | `hooks/useSingLoopPlayer.ts` | ✅ | Dore 播放/评分编排：`doreMediaPlayerLoadWithScoring` 一次性加载+启评分、`GetPitchScore` 取系统分、`reportSingStart/End` 上报、资源未就绪不阻塞（服务端超时兜底） |\n| 文件 | `hooks/useSingLoopPush.ts` | ✅ | Push 订阅（`sing-loop-session-start/round-changed/record-score/team-changed/session-end`）+ 低频轮询兜底（默认30s）+ `funModeType === 'SING_LOOP'` 恢复拉取 |\n| 文件 | `hooks/useResourceDownload.ts` | ✅ | MediaLibrary 批量下载（audio/lyric/pitch），当前轮 priority=100，麦上/发起者下载audio，观众仅lyric+pitch |\n| 文件 | `hooks/useSingLoopBusiness.ts` | ✅ | 抢唱/应战/投降操作封装 + 埋点 + 失败Toast |\n| 文件 | `shared/audioEffect.ts` | ✅ | 抢唱成功/应战/无人音效 |\n| 文件 | `store/appSlice.ts` | ✅ | `selectSingLoopConfigs`（默认：prepareCountdown=3s、waitRespondCountdown=3s、grabReadyCountdown=5s、respondReadyCountdown=5s、grabCountdown=10s、respondCountdown=10s、perfectThreshold=90、scoringLevel=kEasy） |\n\n**⚠️ 需关注的特殊点：**\n\n- **快捷送礼按钮（\"为Ta打Call\"）当前未实现**：`PhaseSinging` 中没有送礼按钮，若为需求硬性项需新增（可挂在 `PopularityScore` 同侧）。\n- 音高线渲染已用 Canvas + rAF + `ResizeObserver`，性能方案成熟，但 `HISTORY_LIMIT=500` 与逐帧 `calcRelativePitchFitSegments` 仍建议弱机型回归。\n- 抢唱/应战按钮 **未做防抖**，仅依赖 `setLoading`（`grab`/`respond` loading 标志）+ 服务端先到先得仲裁，与上游\"点击防抖\"要求存在差异。\n\n---\n\n### 需求4：玩法互斥&异常兜底逻辑实现（⚠️ 部分实现）\n\n| 类型 | 文件路径 | 现状 | 说明 |\n|-|-|-|-|\n| 组件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/services/action.ts` | ✅ | `openRoundSing` 已有互斥校验：已开启轮唱PK / 跨厅PK / 魔法团战 / 全麦PK / 黑叶投票中；并发起前置校验\"上麦或厅主\"；二次确认 `Modal.confirm` |\n| 文件 | `/src/renderer/modules/LiveModule/services/actions.ts` | ✅ | `exitRoom` 内已拦截：`if (isMeSinging \\|\\| isMeRoundSingInitiator) { Toast.warning('轮唱发起中，请先结束游戏，再操作'); return; }` |\n| 文件 | `/src/renderer/modules/PPModule/shared/checkGameCanStart.ts` | ✅ | 存量互斥工具（1v1通话/魔法团战/跨厅PK/投票/蒙面/投屏），**但 `openRoundSing` 未复用** |\n| 文件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/hooks/useSingLoopPlayer.ts` | ✅ | 资源未就绪兜底：不调Dore不播放，服务端超时自动结束进评分；闭麦 Toast；多端互顶（`handleJoinChannelSuccess` 中 `finalize({skipReportScore:true})`） |\n| 文件 | `/src/renderer/modules/LiveModule/submodules/RoundSingModule/hooks/useSingLoopPush.ts` | ✅ | 系统分派埋点（应战超时系统指派）、开局/结束埋点 |\n\n**⚠️ 互斥校验缺口（建议本次补强）：**  \n`openRoundSing` 当前**未拦截**：1V1通话（`app.doreBusinessType === DoreBusinessType.OneVsOne`）、KTV合唱（`showKtvSingToast()` 能力已存在）、PP小游戏（`checkGameCanStart` 已有）、蒙面/投屏（`checkGameCanStart` 已有）、心动时刻/甩地雷/抢皇冠（需确认对应玩法状态字段）。建议将 `openRoundSing` 的互斥逻辑收敛到复用 `checkGameCanStart` + `showKtvSingToast` + 通话状态判断。\n\n**⚠️ 下麦拦截缺口：** 上游要求\"演唱中拦截下麦、最小化\"，当前仅在 `exitRoom` 拦截了退出房间；`seatManage`/`guestManage`（下麦/抱下麦）未对演唱者/发起者做拦截，需确认是否由服务端兜底。\n\n---\n\n## 接口表（以实际代码为准）\n\n> 实际接口前缀 `/singLoop/*`，统一走 `renderer/shared/request`（自动解包 `data.data`）。上游文档中的 `/api/v1/sing-pk/*` 路径不成立。\n\n| 接口名称 | 函数 | 方法 | URL | 参数 | 调用时机 | 兼容说明 |\n|-|-|-|-|-|-|-|\n| 获取轮唱PK全局快照 | `apiGetSingLoopInfo` | GET | `/singLoop/info` | `{ liveId }` | 进房恢复 / Push到达后全量拉取 / 轮询兜底 | 无活跃玩法返回空数据，客户端不展示；`status=ENDED` 时清空 store |\n| 发起轮唱PK | `apiStartSingLoop` | POST | `/singLoop/start` | `{ liveId }` | 点击工具入口→二次确认 | 冲突玩法返回4xx，Toast提示 |\n| 抢唱 | `apiGrabRound` | POST | `/singLoop/round/grab` | `{ singLoopSessionId, roundId }` | 点击「抢唱」 | 服务端先到先得，后到者返回 `{result:'FAILED'}` |\n| 应战 | `apiRespondRound` | POST | `/singLoop/round/respond` | `{ singLoopSessionId, roundId }` | 点击「应战」 | 仅对立队可点 |\n| 演唱开始上报 | `apiReportSingStart` | POST | `/singLoop/round/singStart` | `{ singLoopSessionId, roundId, singRecordId }` | READY 倒计时归零（不等资源） | 触发 GRAB_READY→GRAB_SING |\n| 演唱结束上报 | `apiReportSingEnd` | POST | `/singLoop/round/singEnd` | `{ singLoopSessionId, roundId, singRecordId }` | 歌曲自然结束 / 异常兜底 | 触发 SING→SCORE |\n| 上报系统音高分 | `apiReportSystemScore` | POST | `/singLoop/round/score/system` | `{ singLoopSessionId, roundId, singRecordId, score }` | Dore `GetPitchScore(kAverage)` 后 | 闭麦/异常为0分 |\n| 投降 | `apiSurrender` | POST | `/singLoop/surrender` | `{ singLoopSessionId }` | 点击投降（二次确认） | 投降方判负进结算 |\n| 提前结束惩罚 | `apiEndSingLoop` | POST | `/singLoop/end` | `{ singLoopSessionId }` | 结算期点击「提前结束」 | 正常走服务端倒计时自然结束 |\n| 直播间玩法总状态（恢复入口） | — | GET | `/amusement/info` | — | 进房判断 `funModeType`（`NORMAL`/`SING_LOOP`） | 命中 SING_LOOP 再拉 `/singLoop/info`，避免每进房拉重接口 |\n| 轮唱配置下发 | — | GET | `/business/common/config` | — | 业务公共配置，`singLoopConfigs` 字段 | 含8个参数（见需求3表格），无Groot后台配置项 |\n\n**Push 事件（Topic: `WAVE_AMUSEMENT_SING_LOOP_{appId}_{liveId}`）**：`sing-loop-session-start` / `sing-loop-round-changed` / `sing-loop-record-score` / `sing-loop-team-changed` / `sing-loop-session-end`。\n\n**Dore 引擎 API（经 electronApi bridge）**：`doreMediaPlayerLoadWithScoring`（替代旧 SetScoring+StartScoring）、`doreGetPitchScore(kAverage)`、`doreMediaPlayerStop`、`doreSetClientRole(主播)`、`doreSetKTVRole(kSoloSinger)`、`doreMediaPlayerSetTrackIndex/SetPushTrackIndex(0伴奏/1原唱)`、`onPitchValueUpdate` / `onLineScoreValue` 全角色订阅。\n\n---\n\n## 关键代码位置参考表\n\n| 功能点 | 文件路径 | 说明 |\n|-|-|-|\n| 轮唱PK入口（工具项） | `src/renderer/modules/LiveModule/submodules/LiveToolModule/hooks/useLiveTool.tsx` | 工具列表中的「轮唱PK」项 → `handleRoundSingClick` → `dispatch(openRoundSing())` |\n| 轮唱PK状态管理 | `src/renderer/modules/LiveModule/submodules/RoundSingModule/services/store.ts` | Redux slice、全部 selectors（含 `selectIsInRoundSing` / `selectHasRoundSingPermission` / `selectIsMeSinging` / `selectCanGrab/Respond/Surrender`） |\n| 轮唱PK业务编排 | `src/renderer/modules/LiveModule/submodules/RoundSingModule/services/action.ts` | thunks（start/fetch/grab/respond/report/surrender/end/openRoundSing） |\n| Push+轮询状态同步 | `.../RoundSingModule/hooks/useSingLoopPush.ts` | Topic 订阅、`funModeType` 恢复、30s轮询兜底 |\n| Dore 播放与评分 | `.../RoundSingModule/hooks/useSingLoopPlayer.ts` | 音高/句分回调、finalize 上报、资源未就绪兜底、多端互顶 |\n| 资源批量下载 | `.../RoundSingModule/hooks/useResourceDownload.ts` | MediaLibrary 批量下载 audio/lyric/pitch |\n| 音高线渲染 | `.../RoundSingModule/components/PitchLine/index.tsx`（+`utils`） | Canvas 绘制、头像跟随、高亮段匹配 |\n| 麦位布局与状态机 | `src/renderer/modules/LiveModule/submodules/SeatModule/components/ViewTransition/transitionFunc.ts` + `views/index.tsx` | `deriveSeatViewMode` 推导 `roundSing` 模式，`RoundSingPanel` 挂载点 |\n| 房间/麦位入口外显 | `src/renderer/modules/LiveModule/views/index.tsx` | `isInRoundSing` 时 `BroadcastModule` 下移 `top-[200px]` |\n| 演唱/发起拦截 | `src/renderer/modules/LiveModule/services/actions.ts` | `exitRoom` 内 `isMeSinging \\|\\| isMeRoundSingInitiator` 拦截 |\n| 玩法互斥工具（存量） | `src/renderer/modules/PPModule/shared/checkGameCanStart.ts` | 1v1通话/团战/PK/投票/蒙面/投屏（**待接入 openRoundSing**） |\n| 轮唱配置项 | `src/renderer/store/appSlice.ts` | `selectSingLoopConfigs` + `defaultSingLoopConfigs` |\n\n---\n\n## 本次 PC 改造建议动作清单（按优先级）\n\n1. **对齐互斥校验**：`openRoundSing` 补全 1V1通话 / KTV合唱 / PP小游戏 / 蒙面 / 投屏 / 心动时刻等拦截，复用 `checkGameCanStart` + `showKtvSingToast`。\n2. **明确入口范围**：与产品确认是否需要\"键盘TAB/点歌台TAB/公屏游戏卡片/房间信息流\"多入口；如需则按需求1表格新增点位（当前只有直播工具入口）。\n3. **权限口径对齐**：确认是否按\"点唱厅 / 官方厅白名单 / 签约主播 / 麦上用户\"做差异化入口显隐（当前仅权限key + 上麦/厅主）。\n4. **补全快捷送礼按钮**：演唱环节\"为Ta打Call\"按钮当前缺失（若为硬性需求）。\n5. **麦位声浪兜底**：`UserSeat` 增加轮唱场景下\"用户未佩戴声浪→点唱房默认声浪\"的兜底逻辑。\n6. **回归验证**：送礼弹道偏移、进场横幅偏移、音高线性能、弱网资源未就绪、多端互顶等成熟逻辑。\n\n---\n\n*文档输出完成。以上所有路径均已对照 `/Users/lizhi/Desktop/work/lizhi_wave/producer-platform-electron` 仓库实际代码验证，上游文档中 Vue 路径、`/api/v1/sing-pk/*` 接口、`feature/live-room-sing-pk-v1.0` 分支、Groot/BMS 页面在本仓库中均不存在，已按实际代码修正。*\n\n---",
      "document_id": "CP1YdXZmAofmEmxuLcBcCXoYnJd",
      "revision_id": 13
    }
  },
  "_notice": {
    "update": {
      "command": "lark-cli update",
      "current": "1.0.80",
      "latest": "1.0.84",
      "message": "lark-cli 1.0.84 available, current 1.0.80, run: lark-cli update"
    }
  }
}

